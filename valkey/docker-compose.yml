services:
  valkey:
    image: valkey/valkey:9-alpine
    container_name: valkey
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - "./valkey_data:/data"
    network_mode: bridge
