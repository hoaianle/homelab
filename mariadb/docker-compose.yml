services:
  mariadb:
    image: mariadb:lts
    container_name: mariadb-lts
    environment:
      MYSQL_ROOT_PASSWORD: "${ROOT_PASSWORD}"
    ports:
      - "3306:3306"
    volumes:
      - "${MARIADB_DATA}:/var/lib/mysql"
      - "${BACKUP_TOOL}:/mysql-backup"
    network_mode: bridge
