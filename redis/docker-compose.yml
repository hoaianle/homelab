services:
  redis:
    image: redis:alpine
    container_name: redis-alpine
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - data:/data
    network_mode: bridge

volumes:
  data:
